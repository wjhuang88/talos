# v0.10.0 GitHub And Crates Publication

Status: Active

## Authorization And Scope

The maintainer requested consolidation of existing local development into main,
then GitHub and crates.io publication on 2026-09-20. The maintainer explicitly
waived a separate claim PR for this consolidation and subsequently selected the
ADR-080-compatible minor version, v0.10.0, instead of v0.9.3. This is a bounded
maintainer exception to claim sequencing, not a waiver of tests, independent
permission/security/API and release review, exact-head CI, or merge-time CAS.

Base: f92a99c0d696076f06e249b7064659ec4a931770.

The candidate includes existing local Auto diagnostic, byte-budget and accepted
ADR-064 timing corrections, plus merged work since v0.9.2. Previously merged
I279 animation changes are not reapplied. MODEL-007 scheduling records are
preserved but no model-catalog implementation is activated.

## Delivery Gates

- [ ] Consolidated code passes locked workspace preflight and independent security/API review.
- [x] Workspace/internal versions, lockfile, bilingual README/site and release notes agree.
- [ ] Stable candidate passes exact-head CI and independent release review; merge-time CAS passes.
- [ ] Merged target passes `./scripts/release_preflight.sh v0.10.0` before annotated tagging.
- [ ] GitHub release completes with five platform archives and verified checksums.
- [ ] Only registry-enabled workspace crates publish, in dependency order, after GitHub completion.
- [ ] Registry visibility and external CLI/runtime consumption are verified.
- [ ] Final evidence and Board state are synchronized; temporary preservation stash reconciled.

## Boundaries And Recovery

Never publish `talos-models`, `talos-desktop`, or any other manifest with
`publish = false`. Desktop mock delivery is not a released Desktop product.
Do not move a failed tag, overwrite published versions, or automatically yank.
Record a failed immutable action and use a new patch version for a source fix.
Never disclose registry tokens. Keep unrelated local changes recoverable until
their exact preservation is verified; do not discard the synchronization stash
merely because `stash apply` partially succeeded.

ADR-080 requires external exhaustive matches on `UiOutput` to handle the new
`ToolActivity` variant. Legacy history events remain exactly once; transient
activity metadata must not be persisted as duplicate history.

## Current Evidence

Main was fast-forwarded from 62976ed5 to f92a99c0. Stashed local changes were
restored; both I279 completion and MODEL-007 scheduling records were retained.
The obsolete untracked I279 plan remains recoverable in the stash; the main
owner is the completed, reviewed version. Local static independent Agent-role
security review found no blocker; final commit binding remains pending.
No tag, GitHub Release or crates.io version has been created for this release.

Local v0.10.0 preflight passed on 2026-09-20 with Rust 1.97.0, locked workspace
check/Clippy/tests, both governance validators (zero warnings), text boundaries,
CI classifier, public site and installer checks. Invocation used
`CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=2 CARGO_PROFILE_DEV_DEBUG=0 CARGO_PROFILE_TEST_DEBUG=0`
with `./scripts/release_preflight.sh v0.10.0` to bound local build storage.
The earlier sandboxed run failed creating the shared skill fixture directory;
the exact test and complete versioned preflight passed outside that sandbox.
This does not replace final exact-head CI or merged-main preflight.
