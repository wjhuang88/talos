# Release 0.7.0 Closeout

**Status**: In progress — release commit prepared, pending preflight and tag (2026-08-06)

## Objective

Publish `v0.7.0` as a minor release after `v0.6.1`, carrying the accepted I169 Transactional
Batched Steering Turn (TUI-044 / ADR-056) and its durable structured queue, receipt-based
ownership transfer, generation-safe lifecycle, Actor arbitration, exact request-plan boundary,
and transcript-first finalization on `main`.

## Release Boundary

- Includes commits reachable from `main` at the release commit (to be recorded after commit).
- Includes synchronized workspace/internal dependency versions and EN/zh-CN release surfaces.
- Includes five platform archives plus SHA-256 checksums.
- Excludes crates.io publication, stable 1.0 SDK guarantees, and any REL-002 readiness claim.

## Validation And Publication

- `./scripts/release_preflight.sh v0.7.0` passed on macOS with Rust 1.97.0.
- Governance validation and `git diff --check` passed before the release commit.
- Annotated tag `v0.7.0` points to the release commit and was pushed without force.
- The one-shot GitHub workflow completed all five platform builds, generated checksums, and
  published the GitHub Release.

## Evidence

- Workflow: (to be recorded after push)
- Release: [v0.7.0](https://github.com/wjhuang88/talos/releases/tag/v0.7.0)
- Release commit: (to be recorded after commit)

## Failure Rule

If any published artifact is later found invalid, preserve `v0.7.0` and publish a new
patch version after correcting source. Never move or force-push a release tag.
