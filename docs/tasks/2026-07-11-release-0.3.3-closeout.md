# Release 0.3.3 Closeout

**Status**: In Progress

## Objective

Publish `v0.3.3` as the patch release following `v0.3.2`, with workspace and component manifests
using one synchronized version.

## Scope

- Bump the workspace package version and every internal path dependency declaration to `0.3.3`.
- Update English and Chinese release-boundary documentation.
- Validate the workspace before tagging.
- Create and push the annotated `v0.3.3` tag; the tag-driven workflow creates release artifacts.

## Non-Goals

- No crates.io publish.
- No manual GitHub Release asset upload.
- No permission, sandbox, storage-default, or installer behavior changes.

## Validation Plan

- `cargo fmt --all -- --check`
- `cargo check --workspace`
- `cargo clippy --workspace -- -D warnings`
- `cargo test --workspace`
- `scripts/validate_project_governance.sh .`
- `git diff --check`
- `cargo run -p talos-cli -- --version` prints `talos 0.3.3`

## Execution Log

| Date | Step | Evidence | Result |
|---|---|---|---|
| 2026-07-11 | Version bump prepared | Workspace package and internal path dependency versions changed to `0.3.3`; README release boundary synchronized. | In Progress |
