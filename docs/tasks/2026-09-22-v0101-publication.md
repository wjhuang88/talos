# v0.10.1 GitHub And Crates Publication

Status: Complete / Closed (2026-09-22)

## Collaboration Claim

| Field | Value |
|---|---|
| Claim State | Closed |
| Responsible Actor | @wjhuang88 |
| Executing Agent | Codex release session |
| Work Slice | v0.10.1 version preparation, GitHub-first publication, crates.io publication and verification. No product implementation. |
| Claimed At | 2026-09-22 |
| Source Issue | None |
| Governance Claim PR | #594 |
| Authorization Mode | Single-maintainer merge |
| Authorization Evidence | Maintainer requested GitHub and crates.io publication; #594 merged as ab71e02a after independent exact-head release review and CI. |
| Implementation PR | #594 |
| Last Updated | 2026-09-22 |
| Handoff / Release Condition | Closed after GitHub assets, registry visibility and package verification; dependency warning retained below. |

## Candidate Validation Checkpoint

Local versioned release preflight passed before candidate `2ccc2181` was committed.
CI run `35682291282` then detected that this new owner lacked a Collaboration Claim:
the untracked owner had not been included in the local history-based claim check.
This candidate correction records the claim explicitly; no tag or publication has occurred.
The initial preparation incorrectly assumed an existing target-branch release claim;
that assumption is withdrawn. #594 establishes this bounded release record atomically,
and irreversible execution remains gated on its merge and merged-main preflight.

## Authorization And Scope

Maintainer requested GitHub and crates.io publication on 2026-09-22 before further
Desktop work. Release execution follows RELEASE-WORKFLOW and the release-task
exception in AGENT-COLLABORATION. Base: `7202658d1c9d89796275548225d3a5082deb341c`.
Select compatible patch v0.10.1 after v0.10.0: I280 adds SDK facade exports without
removing old paths; I281 delivers ADR-081 shell assessment and Windows timing fixes.
No new product implementation or Desktop release is authorized.

## Gates

- [x] Synchronize workspace/internal versions, lockfiles, bilingual README/site and notes.
- [x] Run locked local release preflight and review the complete staged diff.
- [x] Obtain exact-head CI and independent release/security review before merge-time CAS.
- [x] Run `./scripts/release_preflight.sh v0.10.1` on the merged target before tagging.
- [x] Publish an annotated immutable tag; verify GitHub assets and checksums first.
- [x] Publish only registry-enabled crates in dependency order; verify registry visibility
      and external CLI/runtime consumption afterward.
- [x] Record final evidence and synchronize Board; keep residuals explicit.

## Boundaries And Recovery

Never publish manifests with `publish = false`, including talos-models and
talos-desktop. Never print credentials, move a failed tag, overwrite published
versions or automatically yank. Source corrections after a failed immutable tag
require a new patch version. A successful upload alone is not full release evidence.

## Preparation

Main was clean and synchronized. Latest GitHub release is v0.10.0. I280 and I281
are merged; MODEL-007 and Desktop real-runtime binding are not activated.
Initial available disk space is approximately 5.1 GiB with 11 GiB in target;
build-space management is required before expensive release validation.

## Completion Evidence (2026-09-22)

Completion Commit: `ab71e02a00138bb5394cd54a661345abefa35fe7`

PR #594 merged this candidate after exact-head CI run `35685557090` passed all six
jobs. Independent release review approved head `652470f0df62528dc65df494d0feaad25a446e2a`
against base `7202658d1c9d89796275548225d3a5082deb341c`; the correction added only the
claim and truthful CI checkpoint. Merged-main `./scripts/release_preflight.sh v0.10.1`
passed with Rust 1.97.0 and locked workspace checks. The sandboxed run failed writing a
shared-skill test fixture outside the workspace; the authorized unrestricted retry exhausted disk
during its fixture build; generated `target/` artifacts were removed and the rerun passed.

Annotated tag `v0.10.1` points to `ab71e02a`. GitHub Release workflow `35692271800`
passed its preflight, all five platform builds and release creation. All five archives and
`checksum.sha256` were downloaded; every SHA-256 entry verified, and the macOS ARM64 CLI
reported `talos 0.10.1`.

All 21 registry-enabled crates were published in dependency order after GitHub Release;
`talos-models` and `talos-desktop` remained excluded. Cargo registry verification from an
independent `/private/tmp` root resolved all 21 packages at `0.10.1`, including
`talos-runtime` and `talos-cli`. The registry emitted a non-blocking warning that locked
dependency `chacha20 0.10.1` is yanked; no published package failed verification.
Cargo publication retained package verification builds. External consumption evidence is
the downloaded CLI execution, external Cargo package retrieval, and package verification;
no separate registry-only SDK executable or `cargo install` acceptance is claimed.

Residual: the yanked transitive dependency should be addressed in a future dependency
maintenance iteration; it does not alter this immutable release.
