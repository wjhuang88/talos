# Release 0.3.4 Closeout

**Status**: In Progress

## Objective

Publish `v0.3.4` as the follow-up patch release after the `v0.3.3` tag's release workflow exposed
an updated Clippy `useless_borrows_in_formatting` failure in the OpenAI provider.

## Scope

- Remove the redundant format argument borrow in `talos-provider`.
- Synchronize the workspace and every internal path dependency declaration to `0.3.4`.
- Validate the workspace with the current Clippy rules before tagging.
- Create and push annotated tag `v0.3.4`; the tag-driven workflow creates release artifacts.

## Non-Goals

- No tag rewriting or force-push of `v0.3.3`.
- No crates.io publish or manual GitHub Release asset upload.
- No permission, sandbox, storage-default, or installer behavior changes.

## Validation Plan

- `cargo fmt --all -- --check`
- `cargo check --offline --workspace`
- `cargo clippy --offline --workspace -- -D warnings`
- `cargo test --offline --workspace`
- `scripts/validate_project_governance.sh .`
- `git diff --check`
- `cargo metadata --no-deps --format-version 1` reports every Talos package as `0.3.4`

## Execution Log

| Date | Step | Evidence | Result |
|---|---|---|---|
| 2026-07-11 | Clippy correction | `talos-provider/src/openai.rs` no longer borrows `self.api_key` redundantly in `format!`. | Pass |
| 2026-07-11 | Version bump prepared | Workspace package and all internal path dependency versions changed to `0.3.4`. | In Progress |
