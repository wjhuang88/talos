# Iteration I162: v0.6 SDK Fixture And Publication Readiness

> Document status: Complete / Review outcome recorded (2026-08-15)
> Published plan date: 2026-07-26
> Planned objective: An external consumer fixture and metadata-derived closure produce an explicit v0.6 publication GO/NO-GO without performing a real publish.
> Baseline rule: once committed, preserve this target; changed targets use a new iteration ID.
> MVP deliverable: An external consumer fixture and metadata-derived closure produce an explicit v0.6 publication GO/NO-GO without performing a real publish.
> Activation rule: this iteration is not implementation authority until its Selected Story is Ready and the activation gate is recorded.

## Collaboration Claim

| Field | Value |
|---|---|
| Claim State | Closed |
| Responsible Actor | @wjhuang88 |
| Executing Agent | `Codex / GPT-5 mainline release-governance session` |
| Work Slice | `ARCH-031-D / I162` only: external consumer fixture, metadata-derived publishable closure, per-crate package and `cargo publish --dry-run` evidence, and explicit GO/NO-GO packet for the candidate v0.8.0 release. No runtime behavior, version bump, tag, GitHub Release, or real Cargo publication. |
| Claimed At | 2026-08-15 |
| Source Issue | None |
| Governance Claim PR | #253 |
| Authorization Mode | Independent review |
| Authorization Evidence | I161 closeout is effective at `main@2301434a`; claim PR #253 exact head `913a3318` passed CI `31886470911`, independent approval `5302451715`, governance validators, and merge-time CAS, then became effective on `main` as `38127228`. |
| Implementation PR | #255 |
| Last Updated | 2026-08-15 |
| Handoff / Release Condition | I162 readiness is complete with a reviewed `NO-GO`; I203 remains blocked until a separate claim and reviewed network-enabled GO packet authorize release work. |

## Published Baseline

### Selected Stories

| Story | Parent | Status At Selection | Depends On | Outcome |
|---|---|---|---|---|
| `ARCH-031-D` | `ARCH-031` | `Refinement/Blocked` | `I161` Complete; versioning review authorizes readiness work | An external consumer fixture and metadata-derived closure produce an explicit v0.6 publication GO/NO-GO without performing a real publish. |

### Start Here

Read in order:

1. `AGENTS.md`
2. `docs/sop/START-ITERATION.md`
3. `docs/sop/ITERATION-WORKFLOW.md`
4. `docs/sop/CHANGE-CONTROL.md`
5. `docs/tasks/2026-07-26-v0.6-runtime-productization-program.md`
6. this iteration
7. the selected Story
8. governing ADRs/specifications in Required Reads
9. the exact source files named by the Story

The selected Story owns scope and acceptance. This iteration owns activation, execution evidence,
variance, and completion state.

### Authorized Scope

- implement external fixture and readiness packet;
- compute actual dependency order;
- run package/dry-run where authorized;
- update distribution and migration docs.

### Forbidden Changes

- no real publish;
- no tag/GitHub Release/product release;
- no publish=false removal from guarded crates;
- no talos-models publication;
- no runtime behavior implementation;
- no v1.0 claim.

### Implementation Slices

1. **Baseline**
   - inspect current code and record current behavior;
   - run focused baseline tests;
   - list expected files before editing.
2. **Tests**
   - add failing focused tests for the Story acceptance;
   - do not rewrite unrelated tests.
3. **Minimum implementation**
   - implement the smallest change satisfying the selected Story.
4. **Runtime wiring**
   - prove the external SDK fixture, metadata-derived closure, package checks, and dry-runs.
5. **Documentation and owner sync**
   - update Story, iteration, parent, Board, and user/reference docs named by the Story.
6. **Validation and commit**
   - run focused and full validation;
   - inspect staged diff;
   - create one logical commit referencing `ARCH-031-D` and `I162`.

### Non-Goals

- no real publish;
- no tag/GitHub Release/product release;
- no publish=false removal from guarded crates;
- no talos-models publication;
- no runtime behavior implementation;
- no v1.0 claim.

### Acceptance

All unchecked Acceptance items in `ARCH-031-D` that belong to this iteration must be satisfied. Do not
invent acceptance criteria here.

### Planned Validation

Run all commands in ARCH-031-D, fixture builds, per-crate package/dry-run, and full locked validation.

### Runtime Evidence

Record clean-environment external fixture results and a signed-off GO/NO-GO packet. Explicitly state
that no real publish, tag, or release occurred.

### Documentation To Update

- selected Story;
- this iteration;
- parent Epic/Story if its actual state changes;
- `docs/BOARD.md` derived view;
- `docs/backlog/PRODUCT-BACKLOG.md` compact row if state changes;
- `docs/iterations/README.md`;
- user/reference docs named by the Story.

### Risks And Rollback

- Preserve the previous runnable path until the new path has focused and runtime equivalence evidence.
- Roll back the iteration commit if a security, permission, persistence, or product-mode regression is found.
- Do not hide a failed gate by weakening acceptance or deleting tests.

### Stop And Escalate Conditions

- version bump is not authorized;
- closure reaches a guarded crate;
- registry facts differ from docs;
- package contains unsupported/private assets;
- a real publish/tag/release is requested.

If a stop condition occurs:

1. stop editing;
2. record the exact code/document conflict under Variance And Residuals;
3. keep the iteration `Blocked` or `Review`;
4. do not create a speculative workaround;
5. request maintainer/architecture input.

## Actual Activation And Execution

| Date | Type | Record |
|---|---|---|
| YYYY-MM-DD | Activation | Record dependency inventory, baseline SHA, primary executor/runtime, and activation decision. |
| 2026-08-15 | Activation | Fresh inventory at `main@2301434a` found no Active or Review iteration before activation; I159/I160/I161 Complete, I162 sole new Active iteration, I203 Blocked/Unclaimed, I188/I189/I195/I196 Planned/Claimed and unactivated, and I164 Paused. Claim PR #253 merged as `38127228` after exact-head approval `5302451715`, CI `31886470911`, validators, and merge-time CAS. Implementation may begin from this exact main; no version, tag, GitHub Release, or Cargo publication authority is created. |

## Verification Evidence

- Focused tests: external fixture checks and runs passed in default and `coding` modes; see
  `docs/reference/I162-PUBLICATION-READINESS-2026-08-15.md`.
- Full locked validation: CI run `31891263313` passed 5/5 on exact head `077b347d`; local restricted
  execution had 16 pre-existing CLI discovery permission failures, recorded as an environment residual.
- Runtime evidence: external fixture default/coding runs passed; publication package/dry-runs are
  NO-GO because the available registry/index cannot resolve the 0.7.0 closure.
- Governance validation: project and collaboration validators passed 0 warnings at exact base.

## Completion Evidence

- Completion Commit: `077b347dff25f60e6fbd84b22548f58c72163f65` (pre-existing implementation commit;
  fixture and readiness packet)
- Implementation PR #255 merged as `16564ba01fe69ee95297898c8faab1c1701e5bb2` after exact-head CI
  `31891263313` (5/5 SUCCESS) and independent approval `5302842269`.
- Do not cite a status-only documentation commit as implementation completion.
- Keep `Review`, `Partial`, or `Blocked` if implementation, runtime evidence, CI, or human acceptance is pending.

## Variance And Residuals

- Registry access and compatible 0.7.0 index state could not be established in the execution
  environment. Package and dry-run failures are recorded in the readiness packet; no version,
  guard, or runtime workaround was introduced.
- The candidate is `v0.8.0` per the governing long task, while this historical owner retains its
  published v0.6 wording. The packet records the current candidate without rewriting the baseline.

## REL-002 Execution Record

- Primary executor/runtime: `Codex / GPT-5`, pinned repository toolchain, implementation worktree
  `feat/i162-sdk-readiness` from `main@2891105d`.
- External assistance: independent exact-head review by `GLM-5.3 / OhMyOpenCode` under shared-account
  identity disclosure; approval comment `5302842269`.
- Planning/editing/testing/docs/commit/push ownership: executing agent, with maintainer-controlled
  merge and release actions.
- Qualification verdict: `NO-GO` for publication; fixture readiness evidence is complete, while
  registry/package gates remain blocked. This is a readiness closeout, not publication approval.

## Retrospective

- Outcome: fixture and metadata closure evidence complete; publication qualification is `NO-GO`.
- Documentation: readiness packet and current publication-matrix reconciliation added; Published
  Baseline remains unchanged.
- Lessons: registry/index availability must be treated as an external gate; offline package and
  dry-run results cannot establish publication readiness. Full workspace tests also exposed
  environment-specific `Operation not permitted` failures in existing CLI discovery tests; no
  unrelated implementation change was made.

## 2026-08-15 I162 Execution Checkpoint

The external fixture was added under `tests/fixtures/runtime-sdk-external/` as an independent Cargo
root. Default and `coding` checks and runs passed. Locked metadata computed the 20-member CLI/runtime
closure and the packet records all 16 registry-enabled members plus four intentional guards.
`cargo check --workspace --locked`, `cargo clippy --workspace --locked -- -D warnings`, formatting,
metadata, publication guards, and governance validators passed. `cargo doc --workspace --locked
--no-deps` completed with pre-existing rustdoc warnings. `cargo test --workspace --locked` reached
the existing CLI suite but 16 provider-discovery tests failed with `Operation not permitted` in the
restricted execution environment; this remains a residual validation blocker and is not attributed
to the fixture.

Package and dry-run attempts are captured in the readiness packet. The available registry/index
could not resolve internal `0.7.0` dependencies and network access was unavailable, so the packet
is explicitly `NO-GO`. No version bump, tag, GitHub Release, or real Cargo publication occurred.

## 2026-08-15 I162 Closeout Checkpoint

I162/ARCH-031-D is Complete/Closed as a readiness-only result with reviewed `NO-GO` publication
qualification. Completion Commit is `077b347dff25f60e6fbd84b22548f58c72163f65`; implementation PR
#255 merged as `16564ba01fe69ee95297898c8faab1c1701e5bb2` after exact-head CI `31891263313` passed
5/5 and independent approval `5302842269`. The external fixture, metadata-derived closure, guard
audit, package attempts, dry-run attempts, and blockers are recorded in the readiness packet. No
version bump, tag, GitHub Release, or Cargo publication occurred. I203 remains Blocked/Unclaimed
until a separate claim and reviewed GO packet exist.
