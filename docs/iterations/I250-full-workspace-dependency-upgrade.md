# Iteration I250: Full Workspace Dependency Upgrade

> Document status: Active
> Published plan date: 2026-09-07
> Planned objective: Complete the user-requested full dependency-version upgrade generated by I248, with every audited candidate upgraded to latest stable or explicitly dispositioned as current, blocked, or exception.
> Baseline rule: preserve this plan; a materially different outcome requires a new iteration.
> MVP deliverable: A merged, validated dependency upgrade (or an evidence-backed disposition for every candidate) and a later accepted-baseline advancement. Existing default features and `cargo run`/`cargo build` scope must remain unchanged.

## Collaboration Claim

| Field | Value |
|---|---|
| Claim State | Claimed |
| Responsible Actor | @wjhuang88 |
| Executing Agent | Codex / GPT-6 mainline governance session |
| Work Slice | Full workspace dependency-version upgrade from I248 live audit: compatible batches plus isolated major/pre-1.0/native/runtime candidates; no unrelated product work |
| Claimed At | 2026-09-07 |
| Source Issue | #474 |
| Governance Claim PR | #500 |
| Authorization Mode | Single-maintainer merge |
| Authorization Evidence | Atomic claim/activation merged in #500; merge commit `ea9a4c37c129c2eb77825191b9e86baced4c91d4`; implementation authority effective from that commit |
| Implementation PR | Not started (local convergence) |
| Last Updated | 2026-09-07 |
| Handoff / Release Condition | Claim active after #500 merge; implementation starts from `ea9a4c37` or later main after local inventory |

## Published Baseline

### Selected Stories

| Story | Parent | Status At Selection | Depends On | Outcome |
|---|---|---|---|---|
| DEPENDENCY-002 | #474 / DEPENDENCY-001 | Planned / Unclaimed | I248 complete; accepted live audit and candidate handoff | Full direct-dependency upgrade and explicit disposition of all audited identities |

### Scope

- Re-run I248 audit from implementation-time `main` and use its provenance as the candidate source.
- Process all 56 distinct package names / 67 dependency identity rows, including patch/minor, major, pre-1.0, prerelease and multi-resolution cases.
- Upgrade to latest stable by default, including major releases; isolate candidates whose API, runtime, persistence, permission, sandbox, native or platform surface requires it.
- For any candidate that cannot safely land, record `Blocked` or `Exception` with owner, concrete reason, revisit trigger, validation gap and rollback plan; do not silently retain an old version.
- Preserve default feature combinations, workspace membership, public API compatibility contracts unless a migration is explicitly accepted, and existing `cargo run`/`cargo build` behavior.
- Advance `DEPENDENCY-BASELINE` only after implementation evidence is merged and verified.

### Non-Goals

- No unrelated refactors or product behavior changes.
- No automatic `cargo update` acceptance without candidate attribution and validation.
- No release/tag/publication, workspace dependency centralization, or new helper runtime unless separately selected and governed.

### Acceptance

- Every identity in the fresh audit has a final disposition: upgraded to latest stable, current with evidence, or blocked/exception with complete ownership and revisit data.
- Manifest and lockfile changes are attributable to listed candidates; no unexplained transitive churn remains.
- Generic locked validation, affected-domain tests, feature matrix and platform checks pass for each landed batch.
- Public/API, runtime, persistence, permission/sandbox, native and provider impacts receive the domain-specific review required by `DEPENDENCY-UPGRADE.md`.
- Existing default Cargo behavior is regression-checked before and after the upgrade.
- Accepted baseline advancement cites an already-merged implementation commit and does not self-certify.

### Planned Validation

- `bash scripts/dependency_audit.sh --live --baseline docs/reference/dependency-baseline.json --format json` with recorded source SHA/timestamp.
- Full locked format/check/clippy/test and release preflight after local convergence.
- Candidate-specific tests for `rmcp`, `wasmtime`, `gix`, TUI/rendering, network, storage, parser/native and permission/security surfaces as actually affected.
- Both governance validators with explicit `COLLABORATION_VALIDATION_BASE=origin/main`, `git diff --check`, secret/generated-artifact scan and staged-diff inventory.
- Exact-head CI and independent authorization-specific review only for the stable candidate stage, not for each local edit.

### Documentation To Update

- I250 owner, `DEPENDENCY-002`, `DEPENDENCY-UPGRADE.md` only where generic gaps are found, accepted baseline, Issue #474 and derived Board/index/manifest views.
- Per-candidate migration/exception notes and a final disposition table.

### Risks And Rollback

- Risk: API or runtime incompatibility, feature/default-scope drift, platform failure, native panic, or lockfile churn obscures attribution.
- Rollback: revert the complete candidate batch or isolated slice, retain the prior accepted baseline, and record the failed validation and follow-up owner. Never force-push or move a failed tag.

## Actual Activation And Execution

| Date | Type | Record |
|---|---|---|
| 2026-09-07 | Planning | Created after I248/#497 completed. I249's one-package published baseline remains unchanged and is not widened; no Cargo change is authorized before I250 claim activation. |
| 2026-09-07 | Claim proposal | #500 proposes atomic I250 Active/Claimed activation. The proposal is ineffective until merged; no Cargo changes are included. |
| 2026-09-07 | Activation | #500 merged as `ea9a4c37c129c2eb77825191b9e86baced4c91d4` into `origin/main`; I250 claim and Active state are effective. Implementation starts from this merge or later main. |
| 2026-09-07 | Implementation | Fresh live audit queried crates.io from implementation head `9de1290a0cf933c0caec9e67d4741dc5dd596086`; 34 candidate groups generated. Local dependency candidate is converging; no implementation PR has been pushed. |

## Verification Evidence

### 2026-09-07 Claim Preparation And Serial Execution Contract

Inventory base: `ea9a4c37c129c2eb77825191b9e86baced4c91d4`. I248 is Complete/Closed on that
base. I249 remains
Planned/Unclaimed and is explicitly deferred; its one-package baseline is unchanged. I250 is the
selected next iteration. I164 remains Paused/superseded by I165. Issue #499 is tracked independently
by RUNTIME-007 and is excluded from I250. No other current iteration header
declares Active, Review, Planned or Blocked; legacy files without current status and historical
superseded shells confer no authority. No open PR exists at preparation. The detached
`talos-i474-upgrade` experiment and divergent local main are not accepted implementation.

This proposal covers the full outcome. Execute serially, with independently revertible commits
and separately reviewable stable candidates for these surfaces:

| Slice | Scope | Additional validation |
|---|---|---|
| Compatible utility/runtime batch | Fresh-audit compatible candidates, excluding native and persistence changes below | Full workspace and all-feature checks/tests, minimal/default tools/runtime feature matrices, provider cancellation/streaming and permission regression tests |
| Native parser | arborium and its parser graph | talos-text parser/rendering fixtures, panic/containment review |
| SQLite | rusqlite and libsqlite3-sys | consumer validator policy, storage restart/migration/failure tests; preserve bundled configuration |
| MCP | rmcp across CLI/MCP manifests | client/server/transport fixtures, public API assessment and protocol interoperability |
| WASM | wasmtime and graph | wasm-enabled plugin load/trap/resource limits/cancellation tests; enforce MSRV |
| Git | gix across tools/TUI | repository status/diff/revision/worktree fixtures and default-feature comparison |
| Rendering | mermaid-text and tui-markdown | snapshots and terminal acceptance if visible behavior changes |
| Encoding | base64 | provider image/request encoding fixtures |
| Compression | zstd across config/session | read old compressed data, roundtrip/restart/error tests |
| Directory discovery | dirs | home/config-path compatibility tests on supported platforms |
| OS ABI | libc stable/prerelease decision | inspect all consumers and ADR-007 boundaries; mandatory independent security review and platform checks |

Refresh targets before each slice; the 56 names/67 identities are the starting inventory, not a
cap excluding newly discovered identities. Record every final disposition. Large version changes
are attempted and independently validated; compiler failures are investigated, not automatically
converted into exceptions. Preserve workspace MSRV 1.95 separately from pinned compiler 1.97.0;
any required public Talos API or MSRV policy change needs an accepted decision before landing.
Do not use `--ignore-rust-version` as acceptance evidence. Keep workspace membership, default
members, default features, and normal run/build selection unchanged.

Issue #499 (pre-Provider rejected submission terminalization) was reported after this plan and is
runtime/session lifecycle work, not dependency-upgrade scope. It remains an independent backlog
item and must not be folded into a dependency batch or used to alter I250's Work Slice. If its
implementation changes a dependency, that change requires its own candidate attribution and
validation under this iteration or a separate governed child.

Allowed production files after claim merge: root/member Cargo manifests and lockfile, affected
dependency consumer code/tests required for migration, existing dependency-sensitive validators,
and directly related migration/baseline docs. No unrelated feature work, centralization, release
or publication. Each candidate records its changed-file inventory and dependency rationale.
Permission/sandbox/process-hardening changes require independent security review before merge;
all slices require fresh CI and applicable independent API/domain review. The final baseline is
generated only from merged validated dependency state. Source-only rollback is insufficient for
data-format changes unless old-reader compatibility is proven.

- Active I250 claim; local dependency candidate is converging from fresh audit source `9de1290a0cf933c0caec9e67d4741dc5dd596086` (observed `2026-09-07T09:57:18Z`).

## Completion Evidence

- Completion Commit: pending

## 2026-09-07 Fresh Audit And Local Upgrade Candidate

The live audit was run after claim activation against implementation head
`9de1290a0cf933c0caec9e67d4741dc5dd596086`:

```text
bash scripts/dependency_audit.sh --live --baseline docs/reference/dependency-baseline.json --format json
```

The registry was queried successfully. Evidence is preserved in
`docs/reference/dependency-audit-2026-09-07.json`, the provenance-wrapped observation in
`docs/reference/dependency-observation-2026-09-07.json`, and the generated candidate grouping in
`docs/reference/dependency-upgrade-candidates-2026-09-07.json`. The report contains 67 identity
rows and the candidate generator emits 34 name/source groups (duplicate identities remain
visible). The source timestamp is `2026-09-07T09:57:18Z`.

The local candidate updates direct workspace declarations and `Cargo.lock` for the following
validated groups: `anyhow 1.0.104`, `arborium 2.18.2`, `async-trait 0.1.92`, `base64 0.23.1`,
`clap 4.6.6`, `dirs 7.0.0`, `futures 0.3.34` family, `gix 0.87.1`, `glob 0.3.4`,
`grep-matcher 0.1.9`, `grep-searcher 0.1.17`, `ignore 0.4.33`, `mermaid-text 0.57.0`,
`regex 1.13.1`, `rmcp 3.2.0`, `rusqlite 0.40.2`, `rust-websearch 0.1.2`, `schemars 1.2.2`,
`serde 1.0.229`, `serde_json 1.0.151`, `similar 3.2.0`, `thiserror 2.0.20`,
`tokio 1.53.1`, `tokio-stream 0.1.19`, `tokio-util 0.7.19`, `toml 1.1.5`,
`tui-markdown 0.3.9`, `uuid 1.26.0`, `wasmtime 48.0.1`, `yaml_serde 0.10.7`, and
`zstd 0.14.0`. Existing broad requirements whose lock resolution is unchanged are retained only
where no consumer-facing migration is needed; the lockfile is the exact resolution authority.

| Identity / group | Final disposition | Evidence or follow-up |
|---|---|---|
| 33 groups listed above | Upgraded | Direct declaration and/or locked resolution updated; workspace all-feature check passed. |
| `libc` (`1.0.0-alpha.3` → stable `0.2.189`) | Exception / blocked | Residual owner: Issue #502, routed through ARCH-034-R04 native-boundary containment. The alpha API is used at the OS-ABI boundary under ADR-007; changing major API semantics requires a dedicated compatibility and security review. Validation gap: no API migration matrix, escape-vector review, or Unix/Windows ABI evidence exists for replacing this prerelease API. Revisit by 2026-12-07 or the next workspace dependency upgrade, whichever is earlier, after ARCH-034-R04 assigns a protected child implementation. Rollback/containment: retain the currently tested alpha resolution; any future failed migration restores its manifest/lock changes before merge. |
| Registry/security/deprecation signals | Exception / unknown | Residual owner: Issue #502, with DEPENDENCY-001 as the recurring audit-policy reference. crates.io version lookup does not provide advisory or deprecation clearance, so the validation gap remains explicit rather than being inferred safe. Revisit on every live audit and no later than 2026-12-07, integrating a Rust-native advisory/deprecation source only through a separately claimed change. Rollback/containment: preserve `unknown` in the accepted baseline and reject tooling changes that silently convert missing evidence to `safe`. |

The remaining verification is full locked test/clippy/preflight, feature/default behavior comparison,
and exact-head review. This section is execution evidence, not completion evidence; the owner stays
`Review / Claimed` until the candidate is merged and the accepted baseline advances.

## Variance And Residuals

- Any candidate not upgraded must be listed with an owner, reason, revisit trigger and rollback plan; an empty or implicit disposition is invalid.
- The two exception rows above are the complete I250 residual set. They identify an existing owner,
  a dated trigger, the missing validation evidence and containment/rollback behavior; neither
  exception authorizes implementation under I250.

## Retrospective

- Outcome: pending
