# I116 State Truth Trace Matrix (2026-07-12)

> Origin: LT010 / N100 — code-to-owner reconciliation for recently delivered work.
> Rule: every claimed status must have a concrete code, test, or runtime reference.

## Summary

Three owner-state drifts were discovered and reconciled. All upgrades are backed by code evidence
and passing tests — no status was changed to preserve a schedule.

## Iteration Status Trace (I106-I115)

| Iteration | Owner Status | Code Evidence | Test Evidence | Runtime Evidence | Verdict |
|---|---|---|---|---|---|
| I106 | Complete | `scripts/talos_smoke.sh` (SBT102), evidence schema in I106 doc | smoke harness passes | `talos_smoke.sh target/debug/talos` exit 0 | ✅ Agree |
| I107 | Complete | `dispatch_timeout_secs` in `talos-config/src/types.rs`, OpenAI SSE timeout in `talos-provider/src/openai_sse.rs` | 4 provider timeout tests + 2 bridge tests | N/A (external runtime) | ✅ Agree (non-qualifying REL-002) |
| I108 | Complete | ARCH-032 audit in `docs/reference/ARCHITECTURE.md` Channel Topology Audit section | zero broadcast channels verified | N/A | ✅ Agree |
| I109 | Complete | `docs/reference/REL-002-READINESS-REPORT-2026-07-09.md` | N/A (report) | N/A | ✅ Agree (NO-GO preserved) |
| I114 | Complete | TUI-028 #24 cadence, #25 ripple, #31 compact status in `talos-tui/src/scrollback_status.rs`, `scrollback.rs` | TUI test suite | Alacritty PTY evidence (I114 doc) | ✅ Agree |
| I115 | Complete | ADR-039 accepted; `talos-session/src/segment_chain.rs`, `compaction_engine.rs`, `compression.rs` | workspace tests pass | binary E2E | ✅ Agree |

## Backlog Status Trace

| Story | Owner Status Before | Code Evidence Found | Test Evidence | Reconciled Status | Action |
|---|---|---|---|---|---|
| SESSION-004 | Ready for Implementation | `CompactTextSessionStore` in `types.rs:354`, `compaction_engine.rs`, `segment_chain.rs`, `compression.rs` (zstd via ADR-036/037) | 145 session tests pass | **Complete** (implementation landed; JSONL legacy read compatibility preserved) | Update backlog + Board |
| PERF-001 | Partial (Phase 2 only) | Phase 1: `generate_compiled_models()` in `talos-config/build.rs:311` parses `models.toml` at build time into `OUT_DIR/models_data.rs`; Phase 2: `talos-tools/build.rs` parses `bash_permission_policy.toml` at build time | 46 config tests pass (builtin_models from compiled data) | **Complete** (both phases delivered) | Update backlog + Board |
| TOOL-020 | Planned | `base_ref`/`head_ref` ref-to-ref comparison in `talos-tools/src/git.rs:386,439` | tools tests pass | **Complete** (ref-to-ref git diff delivered) | Update backlog + Board |
| HOOK-001 | Planned (first diagnostics slice delivered via I091) | `/hooks` command in CLI, `HookRegistry::registrations()` in plugin crate | CLI smoke passes | **Partial** (diagnostics slice delivered; executable carriers remain gated) | ✅ Agree — no change |
| SESSION-002 | Complete | Session integrity tests in `talos-session/src/` | 145 tests pass | Complete | ✅ Agree |
| PERM-004 | Partial (first file-write trust slice) | `WorkspaceTrustStore` in `talos-permission/src/workspace_trust.rs`, trust auto-allow in `lib.rs` | permission tests pass | Partial (PERM-005 remains) | ✅ Agree |

## Verification Commands

```bash
# Session format (SESSION-004)
grep -n "CompactTextSessionStore\|tlog" crates/talos-session/src/types.rs
cargo test --locked -p talos-session  # 145 passed

# Models.toml build-time compilation (PERF-001 Phase 1)
grep -n "generate_compiled_models" crates/talos-config/build.rs
cargo test --locked -p talos-config -- model  # 46 passed

# Bash policy build-time compilation (PERF-001 Phase 2)
grep -n "bash_permission_policy" crates/talos-tools/build.rs

# Git diff ref-to-ref (TOOL-020)
grep -n "base_ref\|head_ref" crates/talos-tools/src/git.rs
cargo test --locked -p talos-tools  # passed
```

## Residual Owners After Reconciliation

- I085 remains Paused (MC107 real-terminal `/connect` walkthrough requires interactive terminal).
- I106-I109 remain non-qualifying for REL-002 (external runtime).
- PERM-005 remains the command-execution sandbox security owner.
- No status was upgraded without direct code + test evidence.
