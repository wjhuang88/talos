# I204 v0.8.0 Release-Candidate Readiness Packet

Date: 2026-08-16
Activation baseline: `main@dbc84f15687c4e199e8bb7a024cf682eded72732`
Claim: I204 claim PR #257, effective at `main@e6cab51a61c3a23a1e0a6792573bcef688a3d6dd`
Evidence branch: `docs/i204-v080-readiness-evidence`

## Boundary

This packet records candidate-only v0.8.0 readiness evidence. It does not authorize a version
bump on `main`, a `models.toml` refresh, release-surface changes, a tag, a GitHub Release, or a
real Cargo publication. The later I203 release implementation owns those changes. `talos-models`
remains `publish = false` and outside the publication closure.

## Reproducible Candidate

The candidate alignment was evaluated in an isolated worktree from the pre-activation claim base.
The exact alignment commit was `5e6ef4d1`; a separate release-surface synchronization commit
`59e7d3ad` was needed only to make the release preflight's README/site version checks coherent.
Both are evidence inputs only and are not ancestors of this documentation branch. Reproduction:

```text
git fetch origin
git worktree add /private/tmp/talos-i204-candidate 59e7d3ad
./scripts/release_preflight.sh v0.8.0
```

The direct checkout is intentional: `59e7d3ad^` is `5e6ef4d1`, so it contains the complete
candidate alignment and release-surface synchronization without replaying conflicting governance
appendix commits onto the later activation main.

The candidate worktree synchronized all workspace and internal path dependency versions to
`0.8.0`; this branch intentionally leaves the published `0.7.0` baseline unchanged.

## Closure And Packaging

Locked metadata produced 20 members in the `talos-cli`/`talos-runtime` closure. `talos-models`
was excluded. Sixteen members are registry-enabled:

```text
talos-agent talos-config talos-conversation talos-core talos-exploration talos-mcp
talos-memory talos-permission talos-plugin talos-provider talos-rpc talos-runtime
talos-sandbox talos-session talos-skill talos-tools
```

Four product crates remain guarded with `publish = false` and were not unguarded:
`talos-cli`, `talos-dashboard`, `talos-evolution`, and `talos-tui`.

`cargo package --list --locked --allow-dirty` passed for all 16 registry-enabled members. Network
dry-run results were:

- Passed: `talos-core`, `talos-exploration`, `talos-memory`, `talos-skill`.
- Expected visibility-gate stop: the other 12 members depend on internal `0.8.0` packages that
  are not yet present on crates.io. This is the dependency-wave condition I203 must resolve after
  the GitHub Release gate; it is not permission to skip packages.

The registry probes were reproducible: `cargo search serde` succeeded, while the
`talos-runtime` crates.io API/sparse-index lookup returned not-found because it has not yet been
published. No upload was attempted.

## External Validation

The independent external SDK fixture passed in both supported modes:

```text
cargo check --locked --manifest-path tests/fixtures/runtime-sdk-external/Cargo.toml
cargo run --locked --manifest-path tests/fixtures/runtime-sdk-external/Cargo.toml
cargo check --locked --manifest-path tests/fixtures/runtime-sdk-external/Cargo.toml --features coding
cargo run --locked --manifest-path tests/fixtures/runtime-sdk-external/Cargo.toml --features coding
```

All four runs printed `talos-runtime external fixture passed`. The full workspace test run also
passed with `TMPDIR=/private/tmp/talos-model-tests cargo test --workspace --locked`.

## Decision

**Conditional GO for preparing the I203 claim; NO authorization for release or publication.**

The candidate package closure, registry reachability, package matrix, and external fixture are
now evidenced. I203 may be claimed only with a fresh exact-main claim and an independent review
of this packet. Its implementation must first run the release preflight against the release
candidate, then create the immutable GitHub tag and complete the GitHub Release before any real
Cargo publish. The `models.toml` refresh patch is reserved for that I203 implementation and must
be reviewed at the release head.

## Residual Gates

- independent exact-head review and CI for this packet;
- fresh I203 Collaboration Claim effective on the resulting `main`;
- release preflight and immutable tag CAS;
- GitHub Release assets/checksum completion before Cargo dependency waves;
- per-package crates.io visibility, external `cargo install talos-cli --bin talos`, and the
  published `talos-runtime` fixture.
