[package]
name = "talos-agent"
description = "Core orchestration logic and the agent turn loop"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true

[dependencies]
talos-core = { version = "0.9.0", path = "../talos-core" }
talos-permission = { version = "0.9.0", path = "../talos-permission" }
talos-sandbox = { version = "0.9.0", path = "../talos-sandbox" }
talos-skill = { version = "0.9.0", path = "../talos-skill" }
talos-plugin = { version = "0.9.0", path = "../talos-plugin" }
talos-memory = { version = "0.9.0", path = "../talos-memory" }
talos-session = { version = "0.9.0", path = "../talos-session" }
async-trait = "0.1"
schemars = "1"
serde = { version = "1", features = ["derive"] }
futures-util = "0.3"
serde_json = "1"
sha2 = "0.11"
thiserror = "2"
tokio = { version = "1", features = ["sync", "macros", "rt-multi-thread", "time", "process"] }
tokio-util = "0.7"
tracing = "0.1"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
serde_json = "1"
tempfile = "3"
tokio = { version = "1", features = ["test-util", "macros", "rt"] }

[target.'cfg(windows)'.dev-dependencies]
talos-tools = { version = "0.9.0", path = "../talos-tools", default-features = false, features = ["shell"] }

[lints]
workspace = true
